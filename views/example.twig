<html>
    <head>
        <link href="{{asset('css/app.css')}}" rel="stylesheet">
        <title>Test</title>
    </head>
    <body>
        <h1 class="example">Hello {{ name }}</h1>
        <ul>
            {% for item in items %}
                <li>{{ item }}</li>
            {% endfor %}
        </ul>
    </body>
</html>